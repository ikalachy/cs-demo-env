{
  "name": "lambda",
  "version": "1.0.0",
  "description": "Lambda proxy func",
  "main": "index.js",
  "scripts": {
    "build": "zip -r lambda.zip .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.2"
  }
}
